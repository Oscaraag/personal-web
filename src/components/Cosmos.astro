---
// Fixed cosmic background: star-field layers are injected by parallax.ts into
// `.cosmos`; `.grain` is a static noise overlay. Both are purely decorative.
---
<div class="cosmos" aria-hidden="true"></div>
<div class="grain" aria-hidden="true"></div>
